# Nera Platform

Short description: TODO

TODO: Add project overview and usage details.

## Development Setup

This repository uses npm workspaces for the monorepo structure.

- Run `npm install` to install workspace dependencies when ready.
- Workspace folders are organized under `apps/`, `packages/`, and `modules/`.
- Environment variables can be configured from `.env.example`.
