# ADR-0003 — Authorization Model (Moved)

**This ADR has moved.** `docs/adr/` is now the single canonical ADR directory (consolidated during Milestone 1 — see `docs/adr/README.md`).

**Canonical location:** [`docs/adr/008-authorization-model.md`](../adr/008-authorization-model.md)

The decision, its context, and consequences are unchanged in substance — only relocated and reformatted to the shared ADR template. **Status was corrected from `Proposed` to `Accepted`** during migration, to match how this decision was already treated everywhere else in the repository (see the canonical file's migration note for the justification). This stub is kept so existing links to this path keep resolving to something meaningful; it is not itself a source of truth.
