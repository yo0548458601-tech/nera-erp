# ADR-0001 — Monorepo Architecture (Moved)

**This ADR has moved.** `docs/adr/` is now the single canonical ADR directory (consolidated during Milestone 1 — see `docs/adr/README.md`).

**Canonical location:** [`docs/adr/006-monorepo-architecture.md`](../adr/006-monorepo-architecture.md)

The decision, its context, consequences, and status (**Accepted**, originally dated 2026-07-09) are unchanged in substance — only relocated and reformatted to the shared ADR template. This stub is kept so existing links to this path keep resolving to something meaningful; it is not itself a source of truth.
